name: flutter_block_then_bug
description: "Reproduction: Block.then() re-entrancy bug with refreshListenable"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.1.0
  # To verify the fix, uncomment the lines below and comment out the line above:
  # go_router:
  #   git:
  #     url: https://github.com/davidmigloz/flutter_packages.git
  #     path: packages/go_router
  #     ref: e0ea78e53

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
